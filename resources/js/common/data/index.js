// Mailbox
import { mailbox } from "./mailbox";

// chat
import {messages, attachementsData, chatContactData, chatData, ChannelsData, callsData, bookmarkData, direactContact, channelMessage} from "./chat";

// Apps > Ecommerce
import { products, customers, orders, sellerOverview, sellers, productsGrid, reviews } from "./ecommerce";

// Apps > Invoices
import { invoicesList } from "./invoices";

// Apps > Learning
import { coursesCategory, coursesGrid, coursesList, instructorGrid, instructorsList, studentsSubscriptionList } from "./learning";

// Apps > Real Estate
import { agenciesList, agentGrid, agentList, earningData, propertyGrid, propertyList } from "./real-estate";

// Apps > ticketList
import { ticketList } from "./tickets";

// Icons
import { remixIcons } from "./remixIcons";
import { materialIcons } from "./materialIcons";
import { bootstrapIcons } from "./bootstrapIcons";
import { phosphorIcons } from "./phosphorIcons";

export { mailbox, products, productsGrid, customers, orders, sellers, sellerOverview, invoicesList, coursesCategory, coursesGrid, coursesList, instructorGrid, instructorsList, studentsSubscriptionList, agenciesList, agentGrid, agentList, earningData, propertyGrid, propertyList, ticketList, remixIcons, materialIcons, bootstrapIcons, phosphorIcons, messages, attachementsData, chatContactData, chatData, ChannelsData, callsData, bookmarkData, direactContact, channelMessage, reviews};